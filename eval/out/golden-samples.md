# Golden chain samples — before/after demo-ready pass

Generated during Phase 2 MOT brain eval. Re-run:

```bash
npm run eval:mot:brain
```

## G2 — Rail orient

**Query:** State of play on rail decarbonisation

**Expected:** substantive route, canvas with verdict + recipe or token-aligned free compose tail.

## G4 — Meta defend

**Query:** Justify your existence. Should CPC be putting money into developing you?

**Expected:** defend canvas, CPC-wide scope — no default rail IncommensurableMagnitudes unless gates pass.

## Improvements this pass

- Flexible recipe ladder (`composition_policy.py` + `recipe_catalog.yaml`)
- Chat rail verdict prepend on substantive turns
- Chart `story` header when present
- Corpus proof panel on provenance click (`corpus_id` → `/api/atlas/projects/[id]`)
