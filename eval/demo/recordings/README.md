# Surface demo recordings

Videos and screenshots from `pnpm run demo:record` (Playwright).

- **Blocked?** See [BLOCKED.md](./BLOCKED.md) until `.env.local` or cloud Secrets are set.
- **Index:** [INDEX.md](./INDEX.md)

Do not commit `.mp4` / large binaries to git unless explicitly requested — use Git LFS or attach to release. This sprint may commit small PNGs only when recorded.
