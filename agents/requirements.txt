# Atlas 5 — Python agent service dependencies
# Managed with uv (uv pip install -r requirements.txt)
# Python 3.11+ required

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# --- LangGraph + LangChain core ---
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-anthropic>=0.1.0

# --- CopilotKit Python SDK ---
copilotkit>=0.1.28

# --- Anthropic SDK ---
anthropic>=0.28.0

# --- Supabase (server-side, service key) ---
supabase>=2.5.0

# --- CPC Corpus MCP — direct Postgres + embeddings ---
openai>=1.0.0
psycopg2-binary>=2.9.0

# --- MCP Python client ---
mcp>=1.0.0

# --- Schema / validation ---
pydantic>=2.7.0

# --- Environment ---
python-dotenv>=1.0.0

# --- HTTP async client ---
httpx>=0.27.0

# --- External Evidence Router ---
exa-py>=2.13.0   # Exa neural search — market_discovery / landscape gaps only

# --- AG-UI streaming (LangGraph ↔ CopilotKit/HttpAgent) ---
ag_ui_langgraph>=0.0.35
ag_ui_protocol>=0.1.18

# --- Dev / test ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
langgraph-cli[inmem]>=0.4.0
